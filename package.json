{
  "name": "@authenio/samlify-xsd-schema-validator",
  "version": "1.0.0",
  "description": "Validator module powered by xsd-schema-validator",
  "main": "build/index.js",
  "scripts": {
    "build": "yarn run clean && yarn run cp-schemas && tsc",
    "cp-schemas": "mkdir -p build/schemas && cp -R schemas build",
    "clean": "rm -rf build"
  },
  "author": "tngan",
  "repository": {
    "type": "git",
    "url": "https://github.com/authenio/samlify-xsd-schema-validator"
  },
  "license": "MIT",
  "dependencies": {
    "@authenio/xsd-schema-validator": "^0.7.1"
  },
  "devDependencies": {
    "@types/node": "^12.0.4",
    "typescript": "^3.5.1"
  }
}
